# LINE LIFF app ID — create at https://developers.line.biz/console/
# (Provider → Channel (LINE Login) → LIFF → Add). Set the LIFF endpoint URL
# to your deployed https origin. Leave empty to run demo mode only.
LIFF_ID=

# Port the server listens on (default 3000).
PORT=3000
